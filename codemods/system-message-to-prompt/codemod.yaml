schema_version: "1.0"

name: "langchain-system-message-to-prompt"
version: "0.1.0"
description: "Replace SystemMessage(content=...) with string in create_agent/create_react_agent calls"
author: "Alex Bit <alex@codemod.com>"
license: "MIT"
workflow: "workflow.yaml"
category: "migration"

targets:
  languages: ["python"]

keywords: ["langchain", "migration", "system-message", "agent"]

registry:
  access: "public"
  visibility: "public"

